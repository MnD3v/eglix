# Configuration Render pour corriger administration_functions
# Ajoutez ces variables d'environnement dans votre dashboard Render

# Variables d'environnement pour la correction
FIX_ADMINISTRATION_FUNCTIONS=1
APP_ENV=production
APP_DEBUG=false

# Variables de base de données (à adapter selon votre configuration)
DB_CONNECTION=pgsql
DB_HOST=your-db-host
DB_PORT=5432
DB_DATABASE=your-db-name
DB_USERNAME=your-db-user
DB_PASSWORD=your-db-password

# Variables d'application
APP_NAME="Egliox"
APP_URL=https://eglix.lafia.tech
APP_KEY=your-app-key

# Cache et sessions
CACHE_DRIVER=file
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Mail (optionnel)
MAIL_MAILER=smtp
MAIL_HOST=your-smtp-host
MAIL_PORT=587
MAIL_USERNAME=your-email
MAIL_PASSWORD=your-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@eglix.lafia.tech
MAIL_FROM_NAME="Egliox"

# Logs
LOG_CHANNEL=stack
LOG_LEVEL=error

# Instructions d'utilisation:
# 1. Copiez ces variables dans votre dashboard Render
# 2. Adaptez les valeurs selon votre configuration
# 3. Déployez votre application
# 4. Le script de correction s'exécutera automatiquement
